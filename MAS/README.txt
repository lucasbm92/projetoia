Este projeto implementa um sistema de manutenção preditiva utilizando aprendizado de máquina supervisionado via rede neural.

O sistema é composto por um modelo de classificação binária que prevê falhas em equipamentos utilizando dados de sensores e parâmetros operacionais, os quais são pré-processados, balanceados e usados para treinamento do modelo.